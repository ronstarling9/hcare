spring:
  application:
    name: hcare-api
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  threads:
    virtual:
      enabled: true

server:
  port: 8080

hcare:
  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: 86400000
